<div class="tab-pane" id="stride-tab">
  <div class="inner">
    <h4 class="header-underline">STRIDE -
      <span class="actions">
        <%= link_to stride_calculator.stride_project_issue_path(current_project, @issue) do %>
          <i class="fa-solid fa-pencil"></i> Edit
        <% end %>
    </h4>

    <div class="mb-4 content-textile">
      <%=
        markup(
          @issue.fields
            .select { |k,v| Dradis::Plugins::Calculators::STRIDE::V1::FIELDS.include?(k) }
            .map { |k,v| "#[#{k}]#\n#{v}" }.join("\n\n")
        )
      %>
    </div>
  </div>
</div>
